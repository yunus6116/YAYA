import 'package:flutter/material.dart';

class Bagis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('BAgis Yap'),
      ),
    );
  }
}
